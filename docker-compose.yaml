version: '3.8'
services:
  postgres:
    image: postgres:15.3
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=m1finance
    volumes:
      - ./tmpdata/pg_data:/data/db